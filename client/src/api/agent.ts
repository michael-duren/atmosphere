import { Account } from './accountRequests.ts';
import { Song } from './songRequests.ts';
import Preset from './presetRequests.ts';

const agent = {
  Account,
  Song,
  Preset,
};

export default agent;
